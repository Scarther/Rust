[package]
name = "lifetimes_exercises"
version = "0.1.0"
edition = "2021"
description = "Rust lifetime annotation exercises for security programming"
authors = ["Rust Security Bible"]

[dependencies]

[[bin]]
name = "lifetimes_exercises"
path = "src/main.rs"
