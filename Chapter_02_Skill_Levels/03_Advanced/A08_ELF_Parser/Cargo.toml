[package]
name = "elf_parser"
version = "0.1.0"
edition = "2021"
authors = ["Security Researcher"]
description = "Advanced Linux ELF (Executable and Linkable Format) file parser for security analysis"
keywords = ["security", "elf", "linux", "malware-analysis", "reverse-engineering"]

[dependencies]
# Command-line argument parsing
clap = { version = "4.4", features = ["derive"] }

# Colored terminal output
colored = "2.1"

# Hex encoding for display
hex = "0.4"

# Serialization for JSON output
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Error handling
thiserror = "1.0"
anyhow = "1.0"

# Byte order handling (little/big endian)
byteorder = "1.5"

# Memory mapping for large files
memmap2 = "0.9"

[dev-dependencies]
tempfile = "3.8"
